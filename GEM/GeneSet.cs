using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace GEM
{
    /// <summary>
    /// The set of genes representing a dataset
    /// </summary>
    public class GeneSet
    {
        /*
        n                           total number of instances (both training and test)
        attr                        number of attributes (including target attribute)
        %nom = nom/attr             percentage of nominal attributes
        %num = num/attr             percentage of numerical attributes
        cl                          number of classes
        %mvals = mvals/(attr*n)     percentage of missing values
        %irrel                      percentage of irrelevant attribs
        +params of distributions of attribs and correlation matrices
        */

        #region fields & properties



        #endregion

        #region methods


        #endregion

    }
}
